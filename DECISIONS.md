# Decision log

Every architectural decision, with what triggered it and when to revisit. **Superseded
rows are kept deliberately so we do not re-litigate them.** Read this before proposing an
alternative — it may already have been rejected, with a reason.

---

| # | Date | Decision | Why | Status / revisit when |
|---|---|---|---|---|
| **V1** | 20 Sep 2026 | **New codebase.** No code is carried in from anywhere | The factory architecture — GuideGraph, arm-don't-press, distillation — wants its own seams. Grafting it onto an existing shape costs more than laying them fresh | **Firm** |
| **V2** | 20 Sep 2026 | **Data and knowledge packs may be imported; nothing else may.** `data/knowledge`, `data/sources`, `data/guides`, the zone and name tables | Facts about TBC are expensive to assemble and cheap to verify. Code is the opposite | **Firm** — provenance in `data/README.md` |
| **V3** | 20 Sep 2026 | **JevRadio is pixel-encoded** — RGB grid, 4 bits/channel, calibration row, checksum plus sequence counter | Density and decode speed, at a robustness level that survives gamma and capture transforms | **Current** — revisit if organisers object to opaque addons; the fallback is legible digits read by a glyph atlas, which costs cells not correctness |
| **V4** | 20 Sep 2026 | **Claude is everything above System 1, over the subscription** (`claude -p`), **not the Anthropic API.** GLM is the cheap bulk rung | Owner has a subscription and no Anthropic key, and wants the ML takeover to be real rather than aspirational. Forces the wean onto the critical path, which is the correct place for it | **Current** — revisit if a hosted key appears, or if subscription limits block a gate |
| **V5** | 20 Sep 2026 | **The GuideGraph is generated from this server's world DB, then verified by walking** | 6,599 quests and 109,358 spawns with exact coordinates are already on disk. Generated *from* this server beats recorded *on* it. The recorder pass shrinks to routes, hazards and grind boxes — what only walking can answer | **Current** — `ARCHITECTURE.md` §8. Supersedes the hand-authoring mechanism in PLAN §7.6, not its intent |
| **V6** | 20 Sep 2026 | **Retail guide packs are a cross-check, never a source** | Custom servers delete, rename and relevel quests. Diffing pfQuest/RestedXP against the world DB is how those changes announce themselves | **Firm** |
| **V7** | 20 Sep 2026 | **Multi-client input path is deferred**; the architecture keeps it a deployment question | One cursor per Windows session is an OS constraint. Separate sessions, VMs, background input and dongles each cost differently, and one clean client beats ten bad ones for the learning goal | **Deferred** — decide before Gate B. Until then: no shared client state, `hid` backends swappable |
| **V8** | 20 Sep 2026 | **Rungs are named for their job, never their supplier** — `coach/`, `teacher/`, not `jev/jev/` or `claude/` | A vendor-named module forces a rename the moment the vendor moves, and that rename touches contracts | **Firm** |
| **V9** | 20 Sep 2026 | **Label by outcome, not by authorship.** Train only on choices graded good at +60 s | Cloning a teacher caps the student at the teacher's error rate, and we have ground truth. Promotes PLAN §12.1's optional bandit to load-bearing | **Firm** — `ARCHITECTURE.md` §2 |
| **V10** | 20 Sep 2026 | **`situation_key` in the schema from commit one** | It is simultaneously the teacher-dedup key, the counterfactual join, the agreement bucket and the answer cache. Unrecoverable if added late | **Firm** — bin widths versioned separately |
| **V11** | 20 Sep 2026 | **The teacher's preferred output is a durable artifact** — skill draft, graph patch, `on_fail` edge — not an immediate action | A decision helps one client once; a skill helps forever. On a rate-limited teacher that compounds. Inverts PLAN §9.2 | **Firm** |
| **V12** | 20 Sep 2026 | **The radio labels the vision heads.** Every `addon_ok` tick logs `(vision_estimate, radio_truth)` for every shared field | Makes fusion confidence measured rather than guessed, and makes addon-off a continuously validated mode rather than a hoped-for one | **Firm** — `ARCHITECTURE.md` §5 |
| **V13** | 20 Sep 2026 | **One source of truth for anything two components must agree on.** Python defines, Lua is generated | Agreement by convention decays silently; agreement by construction cannot | **Firm** — hand-editing `Fields.lua` is a build error |

| **V14** | 20 Sep 2026 | **The teacher's model is pinned, and pinned down: Sonnet, never the CLI default** | An unpinned `claude -p` inherits the user's default, which here is Opus — the scarcest model on the plan and the one the human is using to build with. A farm of ten clients would quietly consume the developer's own capacity. Measured: unpinned calls returned 429 "out of usage credits" while Sonnet and Haiku answered immediately; the *plan* was untouched, only the Opus allocation was spent, and the error message said neither | **Firm** — an explicit model still wins, for escalating a hard postmortem |
| **V15** | 20 Sep 2026 | **Wait for the artifact; discard the stale action** | Measured round trip on a realistic prompt is **52 s** against a 60 s situation bin, so a late answer is the common case. Artifacts describe the *step* and do not go stale; only the fallback action does, and by the time it lands the scripted coach has acted anyway. Discarding whole replies to avoid stale actions would throw away the durable half — which V11 says is the valuable half | **Firm** — `stale_after_s` on the runtime |
| **V16** | 20 Sep 2026 | **Outstanding questions are tracked by the bucket they were asked under** | `situation_key` bins step age, so a step crosses from "fresh" to "slow" at 60 s and its key changes mid-flight. Looking up only the *current* key misses every slow answer, re-asks, and never hits the cache the key exists to enable | **Firm** |

| **V17** | 20 Sep 2026 | **There is no facing API in 2.4.3. Heading is measured, not read** | `GetPlayerFacing` arrived in 3.0; on this client it does not exist, so the field decodes as unknown — confirmed live, and only our own addons reference it while the stock UI does not. Navigation therefore uses **closed-loop turning**: take a heading from a position delta while moving, compute the angle to the target, turn for `angle / turn_rate`, re-measure. Absolute facing is never needed, and a noisy reading costs another iteration rather than a walk into a lake | **Current** — V29 tried to supersede this and did not survive measurement; closed-loop turning is still how this bot aims |
| **V18** | 20 Sep 2026 | **The bot never fights the operator for the desktop** | `SetForegroundWindow` fails from a process that does not own the foreground — measured. `AttachThreadInput` is used so a supervisor can restore a window after a loading screen or a stray alt-tab, but `Hid` refuses to press anything whenever the window is not focused, and that guard is not conditional on it | **Firm** — the guard held on its first live test, refusing rather than typing into the wrong window |

| **V19** | 20 Sep 2026 | **The planner is a standalone Detour sidecar over the server's own mmaps.** `tools/jevpath` links the already-built `libDetour.a` and reads the already-extracted tiles; `MmapQuery` keeps one long-lived process per map | Those tiles are how CMaNGOS walks its own NPCs around Northshire Abbey — the exact problem — and they cost nothing because they were extracted when the server was built. Rejected: **in-process tiles** (no usable Detour binding for Python, and writing one is a project); **CMaNGOS `PathFinder`** (takes a `Unit*` and reaches into the game object model, so it cannot be called standalone — everything it adds over Detour is unit-specific smoothing); **the `.mmap path` GM command** (needs a logged-in character and a chat parser to read its own answer back); **building a second mesh** (a week, to reproduce what is on disk). Long-lived because map 0 is 513 tiles, 2.2 s and 577 MB to load and microseconds to query | **Current** — revisit if a leg needs a route the mesh does not have, which is what `RecordedQuery` is the seam for |
| **V20** | 20 Sep 2026 | **Waypoints are the route, so intermediate arrival is *tight*, not loose** | The obvious choice is wrong. Detour emits waypoints at polygon portals, which around a building come as close as **1.9 yards** apart, so an 8-yard "close enough" radius marked most of a route as already reached: the follower skipped ahead, cut the corner, and walked into the Abbey it had just been routed around — eight legs of nine, failing at the wall the planner existed to avoid | **Firm** — 3 yards, and waypoints closer than that are thinned rather than kept |

| **V21** | 20 Sep 2026 | **A heading is never taken across a turn, and the cruise deadband is 22 degrees** | A pulse arcs the character, so a heading window spanning the pulse measures the arc: the reading overshoots, the error flips sign, and the next tick corrects the other way. Measured: 50 turns in 37 s — a pulse every 0.7 s — on a three-point path a person would walk as two straight lines. Ten degrees is a docking tolerance; with the readout quantising at 0.12 yards and a heading needing 1.5 yards behind it, ten degrees is inside the noise on a long leg. After: **8 turns, 11.6 s, no stucks, no detours** | **Firm** — the weave was the controller, never the mesh |
| **V22** | 20 Sep 2026 | **A blocked planned leg re-queries the planner; it does not detour** | `_detour` is the wall heuristic for walking at a raw node. On a navmesh polyline it is the follower arguing with the planner, and it showed up as three detours on a route already solved. Unstick still runs — a fence is a fence — but the way round a building comes from the mesh, which knows about the door | **Firm** — `to(allow_detour=False)`, `follow(replan=...)` |

| **V23** | 20 Sep 2026 | **The quest log arrives one entry per paint** (`quests.slot` / `quests.count`), replacing a watched-quest field. Schema 2 | 2.4.3 has no "current quest" beyond the watch list and **nothing sets a watch**, so every quest field read as unknown on a live client that had quests in it — and the tracker could not answer "did I accept this?", which is the predicate the whole questing half of the guide rests on. Cycling needs no channel into the addon and keeps it stateless: the decoder reassembles the log from `slot`/`count` over ~2 s at 10 Hz, and `log_hash` says when to discard the assembly | **Current** — `count == 0` is a positive empty log, which is what puts a fresh character on `graph.entry` rather than on whoever is standing nearby |
| **V24** | 20 Sep 2026 | **A character `type_text` cannot send is a failure, not something to skip** | `/` was absent from the key table, so `slash("/reload")` typed **reload** — the addon was never reloaded, a schema bump appeared not to take, and the character announced itself to Northshire. Silent skipping turns a command into a chat message | **Firm** — unsendable characters are recorded and the call returns False |

| **V25** | 20 Sep 2026 | **One frame is never a quest log.** `to_state` returns `None` for any non-empty log; `jev.perceive.questlog.QuestLog` is the only thing that assembles one | The strip cycles one entry per paint, so a frame carries one quest out of `count`. Presenting that as a short log has the tracker find its step's quest absent and fire `QUEST_MISSING` — skipping a live step, twice a second, for as long as the log has more than one entry. Same class as `()` versus `None`, one layer further out. `count == 0` is the single case one frame settles: there are no slots to wait for | **Firm** — `log_hash` discards a half-built assembly rather than mixing two logs |

| **V26** | 20 Sep 2026 | **One locator for every unit on screen**: the selection ring and the nameplate, from one colour-and-components pass. `jev/perceive/units.py` | There is no way to ask 2.4.3 where a unit is, and aiming at a node cannot work because a node is a spawn point and units wander. The ring and plate bracket the model — feet and head — so the midpoint is the torso at any distance, for any unit, with no per-creature knowledge. Fill ratio separates them: a ring is hollow at 0.27, a bar solid at 0.97 | **Both anchors remain required** — measured NPC interactions work, but 22 September wolf frames pair real plates with yellow grass. Ring identity across terrain is not established. The earlier guessed fallback measured 1.27 on one frame and 2.05 on another and clicked Willem's feet |
| **V27** | 20 Sep 2026 | **`target.in_melee` gates nothing that needs interact range** | It is `CheckInteractDistance` index 3 — *duel* range, about eleven yards — while an NPC talks at roughly five. It reads true with the character standing well back, and hours went on right-clicking a model in plain sight and out of reach. Arrival is now the client's own collision: walk forward until the character stops moving | **Firm** — revisit only if a real ~5-yard check can be painted |
| **V28** | 20 Sep 2026 | **Skills get hard caps, and the caps are tested** | The first `Interact` grew a method per failure — preflight, approach, leave_range, turn_to, find_on_screen, locate, a click offset, a fourteen-point sweep — each a patch on the previous symptom. One `/target`, one look, at most one yaw, one click, one confirmation; `tests/test_interact.py` asserts the dead machinery is *absent* | **Firm** — a skill that needs a ninth step needs rewriting, not a ninth step |
| **V29** | 21 Sep 2026 | **Facing is readable in 2.4.3, off the minimap arrow** | The arrow the minimap draws is a `Model`, and `Model:GetFacing()` returns radians (XiconQoo/AVR-TBC, `Core.lua`). The addon polyfills `PLAYER_FACING` over it so `pos.facing` paints as it always claimed to, with **no schema change**. The arrow is matched on its model path, `Interface\Minimap\MinimapArrow`, because the frame name is not guaranteed. **Which object carries the angle is a client setting**: with `rotateMinimap` off the map is north-up and the arrow turns with the player; with it on the map turns and the arrow is pinned pointing up, so `Minimap:GetFacing()` is asked as well and the first to answer wins. This client has `rotateMinimap "1"`. Zero and sign are **measured, never reasoned**: `tools/verify_facing.py` walks forward from several headings and compares the painted value against the direction the character actually moved | **Tried and withdrawn** — measured on this client with `rotateMinimap 0` and a reload, `arrow:GetFacing()` returns **0.0 at every heading**: five walks, direction of travel spread across 160°, painted value unmoved. A constant is worse than nil because a navigator acts on it, so `pos.facing` paints unknown and **V17 stands**. The arrow may not be the model that carries the angle; settling that needs the addon to record what the minimap's children actually are into SavedVariables and read it off disk — client-local, no chat. Asking via `/script` is not an option: it types into the chat box, and a refused keystroke mid-line leaves half a command sitting in Say |

| # | Date | Decision | Why | Status / revisit when |
|---|---|---|---|---|
| **V30** | 22 Sep 2026 | **One coach/tracker runtime, one input-owning worker.** The live probe delegates to `jev.run.cli`; `LiveBody` composes the previously measured body methods | The manual probe and simulated runtime had diverged: live traces lacked the guide, decision links and shadow evidence needed for learning. Tracking/recording must continue during long skills, while cancellation must release the old worker before another can press anything. Input and reader checkpoints plus bounded planner reads enforce the shared ownership seam | **Supervised Windows acceptance started** — concurrent recording, service composition and cooperative input release verified live; wolf engagement and a sustained soak remain pending |
| **V31** | 22 Sep 2026 | **Only applied decisions with observed outcomes become examples.** Preserve arm identity/duration, distinguish completion from rejoin, and exclude synthetic/replay sources by default | Rejected proposals could otherwise inherit the fallback's reward, short runs could earn credit without the full observation horizon, and repeated IDs across runs could cross-join. A failed skill cannot inherit a later retry's success. Missing evidence stays missing | **Implemented, verified offline** — promotion still requires representative live evidence |
| **V32** | 22 Sep 2026 | **Generated target identity is structured data.** Graph schema 2 adds creature/gameobject kind and exact name from the local server DB | Notes contain prose and mixed drop sources include gameobjects. Parsing notes or treating every placed objective as a creature silently targets the wrong thing. Existing route geometry and edges remain unchanged; unsupported target kinds and later unplaced objectives stop explicitly | **Implemented** — object locator and multi-target objective generation need their own evidence |

| **V33** | 22 Sep 2026 | **Merchant actions use painted stock buttons and confirmed transactions.** Schema 7 appends exact money, inventory, offers and supply observations; schema 6 retains explicit decoding | Full bags and empty supplies need the same Interact/list composition as quests. Only DB-confirmed, non-quest junk is sold; exact supported food/drink is bought. Inventory and money changes prove a transaction. The addon never sells, buys or presses anything | **Schema 7 installed; repair and water purchase verified live** — junk selling and wider vendor acceptance remain pending; no equipment destruction or speculative restocking |
| **V34** | 22 Sep 2026 | **Objective identity and coordinate frame are explicit.** Schema 3 carries every generated objective target, radio counter mapping, prerequisites and the graph's navigation frame | A later counter must not reuse the first creature's camp; a city map transition must not reinterpret fractions in the old region. Source zone facts are retained while navigation coordinates are normalized. An explicit supported-route derivative lists every exclusion and never rewards excluded quests | **Implemented, verified offline** — unknown targets, counters or coordinate transforms stay unknown; measured navigation methods remain unchanged |
| **V35** | 22 Sep 2026 | **Continuous learning is outcome-gated and revision-scoped.** Whole-run holdout, versioned artifacts, bounded canaries, fresh model-attributed outcomes, promotion and rollback | Agreement with historical actions is insufficient evidence of improvement. Missing coverage, synthetic windows, mixed controller credit and changed graph revisions cannot promote a model. Safety, recovery, combat and services retain the scripted floor | **Native Windows learner verified over 34 actual runs** — no errors, eligible examples or promotions; live improvement remains unproven |
| **V36** | 22 Sep 2026 | **Optional services never own input or block its clock.** Teacher calls and model loading run separately; input ownership, reconnect, durable playhead and watchdog stops stay in the shared supervisor | A subscription outage, slow model file or restart must not freeze the body or reset completed quests. Teacher proposals are retained, not installed as code. Actual quest/XP progress resets the watchdog; moving the playhead through retries does not | **Initial reconnect and cooperative stop verified on Windows** — teacher transport returned a valid reply, but requested model routing and the complete Windows soak remain pending |

| **V37** | 22 Sep 2026 | **Every fix must solve a failure class through a shared, scalable mechanism.** Task identities are data and acceptance cases, never control-flow patches | The owner explicitly rejects quest/NPC-specific fixes, accumulating fallback branches and duplicated implementations. Correct the owning contract across all callers, remove superseded paths, and verify representative cases. Different observed behavior contracts may have distinct composed skills; moving a workaround into configuration is not generalization | **Firm — explicit owner requirement.** `ARCHITECTURE.md` §6 and `docs/ROADMAP.md` apply this gate to every work package |

| **V38** | 22 Sep 2026 | **Jev teaches by playing bounded actions, then yields proven capabilities to a local student.** The guide and existing routines provide objectives and reusable skills; screenshots, controls, exact-server retrieval and recent outcomes inform each action | The owner clarified that direct teacher play and gradual weaning were the intended build. Whole-skill suggestions could not teach a correction to a failing approach. `jev/play` shares the existing input worker and verified primitives, records action and complete-episode outcomes, and evaluates independent runs before shadow/canary handover. Complete episode teacher cost and task throughput govern promotion; failures restore teacher control | **Implemented, verified offline.** Supersedes V11/V15's artifact-only restriction and the mandatory zero-teacher startup assumption in the original architecture. V35/V36 continue for scripted mode; teaching mode releases input during bounded model waits and stops on teacher unavailability. No live competence or promotion claimed; see `docs/TEACHING_LOOP.md` |

| **V39** | 22 Sep 2026 | **The visual teacher transport is explicitly selectable: Claude subscription or GLM vision API.** Both share the same bounded action and outcome contracts | After the Claude quota block, the owner supplied a GLM credential and requested a cheap model and live testing. The default GLM-4.6V-Flash is image-capable and officially listed free. Credentials stay outside Git and run configuration; one request is bounded, cancellable and never silently retried or routed to a paid model | **Current.** Extends V4 for the owner-authorized GLM API; whole-skill, motor-learning and execution semantics do not depend on the supplier |

| **V40** | 23 Sep 2026 | **Facing is turning until the selected unit's own nameplate is on the screen's centre line** (`Targeting.face_selected`), shared by Fight, the FACE_TARGET routine and the tutor. A right-click is not used to aim | Run `20260922T192105-3d5fcf` frames 99-104: after a hover-verified right-click the Attack slot flashed (auto-attack on) while the wolf stood two yards away at the character's side at full health. A right-click starts an attack but never turns the character, so every stride walked a heading nothing had set. The camera sits behind the character, so a unit on the centre line is straight ahead at any distance; selecting one unit fades the other plates, so the bright plate is the target's. Each turn pulse measures the rate it actually produced | **Current** — implemented and tested offline against a simulated world with real plate pixels; needs live confirmation that swings land |
| **V41** | 23 Sep 2026 | **Melee auto-attack is pressed only when observed off.** Schema 9 paints `bars.attacking` (stock Attack-button flash: `IsAttackAction` + `IsCurrentAction`), `target.melee_range` (`IsActionInRange` on Attack) and a UI error held 1.5 s with a counter | The rotation pressed the Attack toggle after the right-click had already started the swing, switching it off. ARCHITECTURE §6 forbids a toggle without its observed state; the radio had no such state. Closing now stops exactly when a swing reaches; a new "facing the wrong way" error re-faces at once | **Current** — Lua executed under the stub; schema 6-8 captures still decode. Needs the addon deployed to the client |
| **V42** | 23 Sep 2026 | **Corpses are clicked where a fresh hover reports the selected unit dead** (`Targeting.click_corpse`), probing under the last plate the unit had alive, then the centre line. The ring-only corpse path is removed | A dead unit has no nameplate, so the nameplate ambiguity that forbids trusting a living unit's hover cannot occur; corpses do not move. Ring detection is what fails on grass. No live corpse click had ever been recorded | **Current** — offline only; the first live kills are its acceptance |
| **V44** | 23 Sep 2026 | **A plate is the target's only when exact hover ownership proves it; it is then tracked.** Kills are proved by health reaching zero or by experience as the target vanishes; a deselected corpse is found by a dead hover of the killed unit's name | Live, 23 Sep: selection did not fade a rabbit's plate; a damaged unit's plate shrank below the shape test; the client cleared the selection at the moment of a kill (XP arriving with it). Brightness, whole-bar shape and a persisting selection were all assumptions the first facing-based kill disproved | **Current** — the kill is measured; corpse-by-name and plate tracking need their first live repetitions |
| **V43** | 23 Sep 2026 | **The tutor replies with one action name from a state-built menu plus flat parameters.** Capability and expected effect are derived locally; the prompt is compact text; transient provider errors are retried with backoff inside the deadline; one corrective re-ask; an unavailable tutor hands the objective to the scripted routine | The first supervised attempt stopped before any action: the nested union and two redundant labels failed validation on every free-model reply, from a 28k-token prompt. Replays of the same scenes now return valid actions (escape on the login dialog, COMBAT_PROFILE beside a wolf) from about 3k tokens. The free tier answers "overloaded" (1305) in bursts; stopping on it would end every run | **Current** — supersedes V38's reply format, not its learning contract. The Z.AI coding-plan endpoint is not used: its terms restrict it to supported coding tools |

| **V45** | 23 Sep 2026 | **Acquisition looks round before Tab, and a plateless Tab pick is located by the selection it draws.** Three quarter turns for a hover-proved plate of the wanted unit; then Tab; a pick with no plate is turned toward by `units.selection_marks` (selection colour new between the frames either side of the key) and walked at, and never walked at without a mark. A selection kept from before a fight is dropped when its plate is not on screen | Plates are drawn only within about twenty yards and the camera shows about a hundred degrees: 43 of 52 fights in run `20260923T100704-98a757` found nothing, while one stale far pick absorbed 28 fights in the run before. Four blind walks at plateless picks found none | **Current** — look-round verified live (empty looks 43 -> 29, no unseen fights); the mark locator is tested offline and not yet exercised live |
| **V46** | 23 Sep 2026 | **A living unit is clicked under its plate when its ring is hidden** (`units.body_candidates`): points at least 12 px below the bar, inside its span, clear of other bars, still authorized only by a fresh owning hover | Eagan Peltskinner stood just beyond the character with his plate clear and his ring hidden by the character's own model; the turn-in failed on "no eligible target geometry". A point below the bar cannot be the nameplate, so the hover that owns the target there is its body | **Current** — verified live: the next session's click opened his quest frame at the first body point |
| **V47** | 23 Sep 2026 | **Quest reward choice is a painted default, overrulable.** Schema 10 paints the choice count, whether one is chosen, and the default's position (usable, then quality, then the earlier item); the advance routine clicks it first and the tutor has `quest_reward` | Complete Quest does nothing until a reward is chosen; three presses on "Wolves Across the Border" changed nothing. The addon still actuates nothing: it reports the stock panel as it reports the advance button | **Current** — verified live: quest 33 handed in with Soft Fur-lined Shoes. Ranking rewards stays a decision the default does not claim |
| **V48** | 23 Sep 2026 | **A body that plays an objective in stages keeps its routine's clock.** While the tutor plays, the teaching episode's own bounds apply; when a scripted routine takes over, the catalog budget starts then. The tutor's transient retries fill its 30 s deadline while a reply can still arrive | A tutor spent 25 s retrying an overloaded provider and the scripted accept, timed from the arm, timed out walking to Marshal McBride. The previous 2/4/6 s retry schedule left 13 of 30 s unused during four consecutive refusals | **Current** — verified live: the next session's scripted accept completed after a tutor fallback |
| **V49** | 23 Sep 2026 | **Falling interrupts a skill only after 2.5 s; a re-plan starts at the height of the route being followed** | Every unstick jump read as falling (0.5-2.05 s measured) and the cancelled jump kept the character on the near side of a fence for 45 s. On Northshire Abbey's back ledge, a start at the destination's floor height snapped through the wall and every re-plan was a straight line into it (27 stuck events) | **Current** — the grace is verified live; the re-plan height is verified against the real navmesh at all five recorded ledge positions, not yet live |

| **V50** | 23 Sep 2026 | **Heals are cast on the caster, and a spell waiting for a target is observed and cancelled.** Heal-role abilities use the self-cast modifier (the saved `modifiedclick ALT SELFCAST`); schema 11 paints `SpellIsTargeting()` as `bars.targeting`; `Targeting.cancel_pending_spell` presses Esc only on that observation, and the tutor may only observe or Esc while it holds | Reported by the operator and found in frames 534-543 of `20260923T110010-adc4a3`: a between-fights Holy Light with a corpse selected waited for a click, its button lit, and three fights selected nothing because every selection click tried to cast it | **Current** — offline; schema 11 is installed and loads at the next login |
| **V51** | 23 Sep 2026 | **The visual tutor uses the Claude subscription: `claude-opus-4-7` at medium effort** (`--teacher-effort`, the CLI's `--effort`). GLM is no longer the configured tutor | Owner's choice after five sessions in which the free GLM tier refused most requests. Measured on saved frames: 5.7 s smoke, 6.4 s decision replay. Calls draw on the same subscription as the owner's own sessions | **Current** — supersedes V39's provider default for teaching; not yet exercised in a live session |
| **V52** | 23 Sep 2026 | **Walking learns where the mesh is wrong.** A leg blocked until the follower's own detour got past records the spot and the widest point of that escape (`RouteMemory`, `var/route-memory.json`); every later route passing the spot goes by that point. A re-plan that returns the same blocked route detours at once | The planner's navmesh is the server's creature mesh: one polygon spans both sides of a Northshire rail fence, and marking stuck spots expensive changed none of three recorded routes. Only the character's own experience locates those spots. Simulated with the real `Travel` (`walk_sim`): a 60-yard fence 42.4 s -> 11.0 s on the second trip, a trunk beside a wall 20.5 s -> 8.8 s, both with no stuck event | **Current** — simulation and offline only; the first live trips build the memory |

| **V53** | 23 Sep 2026 | **Closing to melee is one steered walk that stops at the first resolved swing.** Schema 12 counts the character's own resolved swings from the combat log (`combat.swings`, hits and misses); forward is held while the tracked plate is steered on; past `in_melee` the walk is bounded and a charging unit is met; reach expires after 4.5 s without a swing | The operator watched the stride loop stutter (walk, stand, walk, stand, nudge) because every stride waited on a full facing look and reach was known only from damage. Simulated with the real `Fight`: 25 yards closed in 2.9 s with forward pressed once, stopping at 4.7 yards | **Current** — simulation and offline; schema 12 installed, loads at the next login |

| **V54** | 23 Sep 2026 | **The client gets the addon as one anonymous file that owns no globals.** The sources are module bodies returning tables; `tools/gen_addon_fields.py` builds `StatusStrip/StatusStrip.lua` (each part in its own function scope, comments stripped) under the title "Status Strip" with no author; `--install` moves earlier installs aside. The Lua tests run the built file, and the harness fails any run that sets a global or names a frame | The operator's anti-cheat review: the event runs a custom Warden, and the addon exposed eight distinctive globals, a title naming the project and its own comments to any Lua-global or memory-signature check, from a public repository. The build strips only comments: every part compiles to the same instructions, constants and locals | **Installed** 23 Sep; loads at the next login. Only as private as the repository: the neutral name is committed |
| **V55** | 23 Sep 2026 | **Every hold and control-loop wait is drawn per event**, from the client's `Humaniser`: holds within 15% (`at_most` lowers the top of the draw instead of clamping), waits within 25% (`hid.pace`). The look-round's way, turns (84-96 degrees) and count, and the Tab burst (3-5) are drawn per search. Callers that learn from a hold divide by what was held (`last_hold_s`) | PLAN 2.1, "randomise at the actuator": taps and mouse paths were drawn, but a quarter turn always lasted 0.672 s, a detour 2.0 s, and the loops pressed on fixed periods. Test fakes and the simulator have no humaniser unless given one, so scripted tests stay exact. **Rejected: waypoint offsets.** Simulated, offsets under a yard change nothing (the 22-degree deadband absorbs them); one-yard offsets stuck a bend inside a 2-yard doorway on 5 of 40 trips; varied start headings already spread walked lines 1.2-1.6 yards | **Current** - simulation and offline |
| **V56** | 23 Sep 2026 | **Stuck is less than a heading's worth of travel (1.5 yards) in 1.5 s with forward held, and the way round an obstacle is searched by doubling** (one detour one way, two the other, then four); a detour whose own walk is blocked sends the rest of the search the other way | Found by simulating V55's drawn timing on the recorded fence: 5 of 60 first trips failed. Pressed into a fence 2 degrees off square the character slid at 0.27 yd/s - past the old 0.3-yard "frozen" test, too slow to steer by - for a whole two-minute walk; and flipping side whenever a detour ended farther away oscillated along anything long. Each change alone was no better or only marginal. Together, over 320 drawn walks of eight shapes, 318 arrived (from 311) and none timed out | **Current** - supersedes the side rule in V22's detour; simulation only |

| **V57** | 23 Sep 2026 | **In teach mode a fight the character is in, and death, run their scripted routine without asking the tutor** (`policy.REFLEX_RULES`: `fight.*`, `preempt.dead`, `preempt.ghost`, `preempt.critical`), and a reflex that fails is a death or a lost fight, not a failed attempt at its step | Live run `20260923T172056-54f4d5`: one unanswered 30 s tutor call in a fight took the character from full health to dead without a swing; the release popup, being modal, left the tutor only Esc, which opened the game menu five times. The coach already kept these rules from teacher and learned answers (V35); teach mode did not. A lost defensive fight had also stopped a run as `1 failed attempts` | **Current** - verified live: corpse runs, releases and resurrections ran as reflexes in runs `...173347`, `...174132` and `...175710`; a resurrected character at 50% healed itself four times and won |
| **V58** | 23 Sep 2026 | **A step that fails into a rib is retried once after it, the playhead saves the rib's way back, and a rib with none rescans from the entry** | Quest 15 sat complete in the log across sessions: its hand-in had timed out, the rib led to the step after it (passing the hand-in over for good, with quest 21 behind it), the playhead kept the rib but not the way back, and a rib finishing with nowhere to go ended the guide. The simulated world could not hand in talk-to quests, which is why skipping looked harmless | **Current** - offline and simulated; live: the next session resumed at the hand-in instead of the stranded rib |
| **V59** | 23 Sep 2026 | **A step's timeout counts only time it could act and made no progress**: the clock stops while dead or a ghost, in combat, and while getting closer to the step's position | Counted from entry, the quest-15 hand-in timed out while the character was still walking to Marshal McBride after a corpse run, a fight and a meal (run `...174132`). Every accept and hand-in has 240 s, and a failed hand-in is passed over after its retry | **Current** - offline; a stalled walk still times out |
| **V60** | 23 Sep 2026 | **Grind ribs per level window, chosen by the character's level.** The generator emits the densest cluster in each two-level window; the runtime fails into the rib for the character's own level | The one rib for levels 1-12 was level 5-6 boars (with level 6 spiders), and every Northshire step sent a level 3 character there: three deaths running until the watchdog stopped run `...174132`. Now wolves 1-3 (Northshire), kobolds 3-5, boars 5-7 and 7-9, prowlers 9-11, smugglers 11-12 | **Current** - regenerated graph; only ribs and escape edges changed |
| **V61** | 23 Sep 2026 | **The camera is levelled at the start of any skill while the character is out of combat** | Levelled lazily by the first look, the five-second drag landed in a panic fight at 12% health and outlasted the character (run `...173347`) | **Current** - verified live: `camera.ready retained` at the next fight |
| **V62** | 23 Sep 2026 | **Hostile nameplates are seen: red bars have a measured 5 px floor, fights search red plates, and self-defence turns round once when Tab finds nothing** | Every hostile bar measured 5 px under the red rule (lossless Defias Cutpurse frame; Mangy Wolf and Forest Spider screenshots), below the 6 px partial-bar floor set against 4-5 px yellow flowers. Facing therefore turned in circles past a red plate in plain view, and Fight's own search looked only for green and yellow; the attacker behind was out of Tab's reach (runs `...172056`, `...175710`) | **Current** - offline against the live pixels; candidates still need hover proof |
| **V63** | 23 Sep 2026 | **A body that kills the character again within 180 s of getting up at it is left: up at the Spirit Healer, then home by hearthstone.** A unit whose nameplate does not show (the Spirit Healer's is behind the strip) is talked to where a fresh hover finds it, stepping closer on "You are too far away!"; the healer is asked again when a click opens nothing | A level 3 character stranded in level 6 wolf country got up at its body beside the wolf that had just killed it, four times running (runs `...174132` to `...181209`) | **Verified live** (run `...183905`): up at the Elwynn graveyard's healer on the second try, hearthed to Northshire; the next session repaired and handed in quest 15 |
| **V64** | 23 Sep 2026 | **A blocked approach is stepped round, and a walk with no re-plans left still rounds its next obstacle.** The fight's walk to a unit uses travel's stuck test (under 1.5 yards in 1.5 s; near, two steps that each moved under half a yard), then strafes off the line: first a drawn side for 0.5 s, then twice as long the other way, up to 2 s. A planned leg blocked after the walk's three re-plans are spent goes to the wall heuristic once, as a same-answer re-plan does | Run `20260923T184413-a386ff` at Echo Ridge Mine: the 366-yard walk spent its re-plans on the Abbey's corners and stopped 95 yards short at a pit prop in the mine's mouth; the tutor's fight then walked into the same prop three times for 18 s, a Kobold Laborer in plain view past it, until "never came within reach" and the watchdog ended the run. Simulated with the real `Fight` and `Travel` (`walk_sim`): a unit behind a post, a post walled on the first side, a post within `in_melee` and four walls on one route all reached | **Current** - simulation and offline |
| **V65** | 23 Sep 2026 | **The strip locator also brackets the strip by its markers' inner edges**, which the calibration row makes exact: ten swatches, none a marker colour, lie between them | Inside Echo Ridge Mine the teal cave `(41, 101, 121)` passes the relative cyan test and lies against the strip, so the right marker's run went on fifty pixels past it; every bracket built from the outer edges was wrong and the character stood among Kobold Laborers reading nothing (run `20260923T190938-3b2dd7`). The mask stays relative, as a quarter-gain capture needs | **Current** - offline against the live frame (`live-echo-ridge-mine.npz`) |
| **V66** | 23 Sep 2026 | **A spot no detour gets past is remembered as blocked, with the way it was blocked, and every plan stays clear of it** (`RouteMemory.block`, `AvoidingQuery`): the planner is asked again by points on rings round the spot, and the shortest pair of halves that never comes within 1.5 yards of it heading that way wins | Echo Ridge Mine: the mesh steps onto the platform across a log, and the grass beside it ends in a pocket that defeated every detour (runs `...184413`, `...190938`). Asked offline to stay clear of the crossing, the real mesh went by the platform's front, where the session 16 walk that did get in crossed | **Current** - offline against the real mesh and scripted; not yet live |
| **V67** | 23 Sep 2026 | **Near a point, a heading error over 60 degrees is turned off standing still** | Simulated: a point four to eight yards off at sixty to ninety degrees was circled until the walk timed out, 16 walks of 72, moving the whole time so no stuck test fired; a character following a learned passage circled its point for 295 s | **Current** - simulation: 72 of 72 arrive |
| **V68** | 23 Sep 2026 | **The watchdog's first window without progress fails the step being worked into its own edge; only a second stops the run.** A fight's sidestep that moves nothing backs off two yards | Session 17 stood on crates inside Echo Ridge Mine at 1/12 while fights came back unreachable, and the 300 s teaching watchdog stopped the run before the step's 600 s timeout could fail it into the kobold grind | **Current** - offline |
| **V69** | 23 Sep 2026 | **The tutor's play is judged for what it teaches.** A selection that was dead and is now alive counts as selected even under the same name; the same tutor action three times in a row without effect ends the episode and Jev's routine plays on; every run summary reports what the motor learner holds and what the run added, against its own gates | The operator's alignment check: Jev with a Claude fallback must feed the learner. Measured: 90 motor examples from 9 runs, nothing trained, 14 of 39 tutor episodes successful. Tab was the tutor's most used action and verified an effect in none of 15 presses - 13 changed nothing, and the one that moved from a corpse to a living kobold was not credited | **Current** - offline |
| **V70** | 23 Sep 2026 | **Every character keeps its own playhead.** Schema 13 paints `char.key`, the player's name and realm hashed (FNV-1a, 31 bits); a run reads it before loading anything and keeps `var/playheads/character-<key>.json`, written on every tracked step. A character with no file starts from its own quest log; a strip with no key is refused rather than guessed; another character's state mid-run stops the run without being tracked or saved | One file per installation: a fresh level 1 would have started at another character's quest 21, Northshire's first five quests marked done. The operator's rule: a new character is detected when the bot runs on it, and every character is always remembered exactly where it was | **Installed** 23 Sep; loads at the next login. The existing save moved to Testvii's key |
| **V71** | 24 Sep 2026 | **Motor models belong to a controls generation, not a knowledge one.** The learner groups and gates by capability and key bindings; the knowledge fingerprint (guide, profiles, catalogue, world snapshot) stays on every record for audit only | The fingerprint hashes the guide file's bytes, and training used only the newest group: any guide change - a regenerated guide, the next level band - restarted every capability's corpus at zero. The knowledge shaped which actions the tutor chose, but every label is graded by its observed outcome (V9) and none of it is a model input | **Current** - offline |
| **V72** | 24 Sep 2026 | **A hunt stands where its target spawns.** The generator keeps each cluster's spawn points, nearest the centre first, in `<guide>.spawns.json`; the hunt walks them as a nearest-neighbour tour (points within ten yards merged, twice round), rings being the fallback. Beside the guide, not in it, so the guide's bytes do not change | Northshire's Young Wolves spawn 24 to 170 yards from their cluster's centre; rings at 0, 13 and 31 yards looked 38 times and found nothing, and the level 1-3 grind failed over for want of a wolf (runs `20260923T233909`, `...235518`) | **Current** - offline; the regenerated guide is byte-identical |
| **V73** | 24 Sep 2026 | **Experience arriving as a living selection is replaced is a kill**, in the fight and in the tutor's effect judge | A Timber Wolf at 20% gave way to another unit at full health on the tick its experience arrived; reported as lost, the kill went unlooted and the quest's meat with it | **Current** - offline |
| **V74** | 24 Sep 2026 | **The motor learning service re-reads a run only when its play files change** | Re-reading all forty runs every 30 s took and released the store's lock once per row; a blocking Windows lock gives up after ten seconds, and a cycle failed with `PermissionError [Errno 13]` | **Current** - offline |
| **V75** | 24 Sep 2026 | **An objective step's clock starts again whenever its quest's counters rise** (V59's clock otherwise unchanged) | Twelve Kobold Laborers took longer than the step's ten minutes of searching, eating and tutor calls between fights: the step failed over to a kobold grind at 7/12 while every few minutes brought another kill, and the grind's Kobold Workers were 180 yards away outside the mine (run `20260924T005824-740147`) | **Current** - offline |
| **V76** | 24 Sep 2026 | **The tutor may select an objective unit's corpse** (`select_corpse`: a left click whose hover must prove a dead unit of that kind), offered unless one of that kind is already selected; `select_unit` is described as a living unit | The tutor's only way to a corpse lying unselected was `select_unit`, which proves a living unit, so each "click the corpse to loot it" was refused (twice in run `...005824-740147`). Its loot attempts that did reach a corpse found them already looted by the scripted fight: the effect judge was right | **Current** - offline |
| **V77** | 24 Sep 2026 | **An exploration objective is walked into**: the hunt walks to the trigger's point and succeeds only on the quest's positive complete flag, and the supported route keeps explore quests | Excluded as "no verified executor", The Fargodeep Mine and The Jasperlode Mine took Westbrook Garrison Needs Help! with them. Their triggers are spheres of 10 and 30 yards round the generated points (`dbc_AreaTrigger` 88 and 87), so arriving within travel's few yards is inside | **Current** - offline; not yet live |
| **V78** | 24 Sep 2026 | **A merchant whose body cannot be clicked is passed over for the next nearest that sells what is wanted**, up to three | Dermot Johns stands behind his wagon: his plate selected him, and every point between ring and plate was wagon. The run stopped on its first full bags with Godric Rothgar in plain view beside it (run `20260924T011327-6e5f4b`) | **Current** - offline |
| **V79** | 24 Sep 2026 | **A tutor episode handed to the scripted routine goes back to the policy first when a blocking dialog is up** | The tutor, unable to sell, opened Godric Rothgar's shop and pressed Escape until the game menu opened; the scripted sale behind it refused the modal and the run stopped (run `...012032-0c0c24`). The next session's ABORT_WAIT closed it at once | **Current** - offline |
| **V80** | 24 Sep 2026 | **Grey items sell.** The addon reads an item's quality from its own record when the container withholds it; poor weapons and armour join the sell list (quest references stay excluded); selling every eligible stack is a success even short of six free slots; and full bags with nothing sellable are not asked about again until a slot frees, nor counted as a failed attempt | A sixteen-slot backpack held eight slots of grey items and none could be sold: 2.4.3's container gives -1 for grey trade junk (Melted Candle, Chipped Claw, Ruined Pelt), and grey armour (Frayed Shoes, Unkempt Pants) was excluded outright. Six sessions running stopped at the Northshire merchants within two minutes (runs `20260924T011327` to `...012829`) | **Current** - offline; addon installed with the next client restart |
| **V81** | 24 Sep 2026 | **The tutor is offered Escape only when something can be closed, and is told trading is routine-only** | Unable to click a shop's items, the tutor opened Godric Rothgar's shop and closed it again, then pressed Escape into the game menu and out of it for the rest of its episode (run `...012829-382fd4`) | **Current** - offline |
| **V82** | 24 Sep 2026 | **A selection whose health rises 40% or more between two readings is another unit, even under the same name**, and is judged as V73's moved-on selection: a kill if experience arrives | A Kobold Worker at 19% became another Kobold Worker at full health far off as the first died; a name-only test called it the same fight, which chased the new one until its plate was lost and reported "not visible", the kill unlooted (run `20260924T013702-7f5692`) | **Current** - offline |
| **V83** | 24 Sep 2026 | **A plan starts at the ground the last walk ended on, then the destination's height, then outward in steps to 60 yards either side; the first complete route wins** | The radio paints no height. From beside Northshire's merchant wagons the destination's height snapped the start onto a wagon (five yards of partial path, called arrival 160 yards short, run `...013702-7f5692`); from Echo Ridge Mine's wooden platform, 44 yards above the grind it was going to, every plan was one point (run `...015205-5e57fc`) | **Current** - verified live: the platform's plan came back complete, 1,197 yards |
| **V84** | 24 Sep 2026 | **Escape is never offered to clear a selection** | In a fight it raised 2.4.3's "Blizzard_TimeManager has been blocked from an action" popup instead, four times in one session (run `...015205-5e57fc`) | **Current** - offline |
| **V85** | 24 Sep 2026 | **Walking closer to the step is progress to the watchdog, by new closest approaches only; a step fails into the nearest rib whose window starts within two levels below the character** | A level 5 character failed out of Echo Ridge Mine into the level 5-7 wolves 1,200 yards away, and the watchdog failed that walk over half way; the level 3-5 kobolds beside the mine were passed by (run `...015701-2417ae`). A new step's first distance is a baseline, so fail/rib cycles still cannot reset the window | **Current** - offline |
| **V86** | 24 Sep 2026 | **Quest objects are gathered.** Schema 14 paints `cursor.object_id`, the stock tooltip's first line hashed as names are, only while the pointer is over the world, no unit is under it and the tooltip is fully shown (a fading one names what the pointer left). The grind walks the object's spawn points nearest first, twice round; at each the pointer searches outward from just ahead of the character's feet, right-clicks only where a fresh hover names the object, and judges the take by the objective counter, then a bag slot. Loot objectives on objects rejoin the supported route; quests taken or handed in at objects (a wanted poster, a body) stay out | Milly's Harvest and A Bundle of Trouble were excluded as needing "a measured gameobject locator", taking Grape Manifest with them, and every later zone has collect quests. An object has no nameplate and no mouseover unit, so nothing else on the strip could say one was under the pointer | **Current** - installed. Live, the field has also named units (a Kobold Worker, McBride) for a paint after their mouseover cleared, their tooltip lingering; harmless to object matches, which are by the object's own name |
| **V87** | 24 Sep 2026 | **Quests taken or handed in at an object are opened by the name its tooltip gives, and a quest whose target is an elite is left out of the route** (the generator blocks the requirement: `creature_template.Rank` 1-3) | With V86's tooltip name, a wanted poster or a body is found the way a crate is: stood at, searched for, right-clicked, and its quest window handled as an NPC's is. That returns Find the Lost Guards, Discover Rolf's Fate, Report to Thomas and the two quests behind them. It would also have returned Wanted: "Hogger" - a level 11 elite with his gnolls round him - so elites are now named as out of a solo character's reach. The regenerated guide changed only that one target | **Current** - offline |
| **V88** | 24 Sep 2026 | **The strip paints the selected unit's GUID (`target.guid`, hashed to 16 bits); a changed GUID is a new selection to the tutor's effect judge and a moved-on selection to the fight.** Schema 14 was redefined to carry it, within the hour it had been installed on the one client: the 4-bit header cannot name 15, so the next field needs a revision number in the header first | 7 of the tutor's 29 failed selections landed on the right kind of unit and could not be credited: another Kobold Worker at full health has the same name and health as the one selected. The fight's moved-on kill (V73, V82) had to be inferred from names and health jumps | **Current** - offline; installs with the next client restart |
| **V89** | 24 Sep 2026 | **The tutor's copy of a frame carries a scale along its edges** (a tick and label at every tenth, within 18 px of the border); the recorded screenshot and the learner's features stay the raw frame, and the tutor's image keeps its own hash | The tutor wrote "the Kobold Worker nameplate at (0.65, 0.36)" and sent x 0.98, twice in one session: reading pixels off an unmarked picture, it divided by the wrong width. Its selections were refused as hovers over nothing | **Current** - offline |
| **V90** | 24 Sep 2026 | **The motor learner holds out whole runs until it has both its two held-out runs and its twenty held-out examples** | Exactly two held-out runs were taken, in a stable hashed order, and then required to hold twenty examples: at about five qualified examples a run they never could, and acquire sat at 55 of 60 behind "insufficient disjoint training and held-out examples" however many runs it earned. On the live corpus the split is now 24 training and 31 held-out: training still needs its forty, so a model comes with a few more sessions | **Current** - offline; checked on a copy of the live store |
| **V91** | 24 Sep 2026 | **"Facing the wrong way" right after an aim that put the plate on the centre line turns the character round**; an aim that never settles, on an attacker in melee, is fought blind (V64's rule) | A unit directly behind the character projects onto the centre line as surely as one ahead. The fight trusted the plate, walked forward - away from a Mangy Wolf biting its back - "never came within reach" twice, and the character died at 5% (run `20260924T035309-97796e`); a wolf whose plate drifted 0.18 off centre had been given up at full health the session before | **Current** - offline |
| **V92** | 24 Sep 2026 | **A rib that has killed the character twice is left for its way back; a step's deaths are kept in the playhead; the body is watched for 150 s while the reclaim delay runs; and when the body is a trap and the Spirit Healer will not raise the ghost, it gets up 32 yards short of the body on the graveyard's side** | A level 6 paladin on the level 5-7 wolf rib died six times in two sessions: the rib has no fail edges and its clock stops while dead; the death count started again every session; at the body the 20 s of looking gave up during the reclaim delay; the Spirit Healer answered three right-clicks with nothing, twice; and each time it did get up, at half health among the wolves round its body, it died again (runs `...035309-97796e`, `...041014-a9781c`). A body is reclaimed from inside the server's 39-yard radius and the character stands where the ghost stood | **Current** - offline; the Spirit Healer's silence is unexplained |
| **V93** | 24 Sep 2026 | **A failed step goes to the nearest rib whose mobs are never above the character (window end at or below its level, start within three below), before V85's two-below rule**; and a step's clock stands still while a service or meal runs for it | The level 5-7 wolves killed a level 6 paladin ten times in three sessions; the level 3-5 kobolds were beside the abbey. And a hand-in's 240 s ran out 150 yards from the abbey on a sale and a meal, sending it back to the wolves | **Current** - offline |
| **V94** | 24 Sep 2026 | **Every body is reclaimed 32 yards short of it, on the side the ghost comes from; a right-click nothing answers is stepped in on; and the Spirit Healer's gossip line is chosen** | V92's short reclaim ran only after a quick second death, which a new session never remembers: a level 6 paladin that pulled four Mangy Wolves got up at its body among them and died again, four times in two sessions (runs `20260924T045140-ec8686`, `...050644-f9f9fa`). The Spirit Healer's silence has a cause: the server drops a right-click from beyond five yards without a word (`INTERACTION_DISTANCE`, `Player::GetNPCIfCanInteractWith`), and six hover-proved clicks opened nothing and raised no error. When it does answer, it answers with a gossip (menu 83: one line, "Return me to life."), not the popup the recovery waited for | **Current** - verified live (run `20260924T054447-632295`): the first click silent, one step in, the second raised the popup; up at the Spirit Healer and home by hearthstone. Reclaiming short verified in `...052148-85c63f` |
| **V95** | 24 Sep 2026 | **A hunt walks a camp's lone spawns before its packs, and round a pack when it can** - each spawn with another inside 20 yards (the aggro reach at the character's own level) is 40 yards further on the tour, and so is each packed spawn a leg passes within 10 yards of | The tour began at the spawn nearest the cluster's centre, which in Elwynn's level 5-7 wolf camp has three other spawns inside twenty yards and seven inside thirty: the walk to it pulled four Mangy Wolves onto a level 6 paladin with no ranged pull, and it died (run `20260924T045140-ec8686`). A camp that is all pack is still walked, pack last | **Current** - offline |
| **V96** | 24 Sep 2026 | **A heal waits while the target will die well before the character would** - at the rates this fight has shown: the target's loss over looks the character was not casting, its own over the last six seconds; held only under 0.8 of the time to die, on three seconds of evidence each, and never below 15% health | A level 6 paladin between two Mangy Wolves healed at 44% with its target at 18%, two swings from dead: Holy Light is a 2.5 s cast that stops the swings and pushback made each one about four, the target sat at 18% through two of them, the mana ran out, and it died with both wolves alive (run `20260924T050644-f9f9fa`). Killing one first halves what a heal has to outpace. A role rule, not a class one | **Current** - verified live: ten `heal.held` in run `20260924T053651-ac99b2`, a Forest Spider at 6% finished instead |
| **V97** | 24 Sep 2026 | **An item onto a stack is loot: a moved inventory revision counts as a take, in the loot routine and in the tutor's `loot_received`, unless food or drink went down** | Money, a quest counter or a newly filled bag slot were the only evidence, and an item that lands on a stack it already has moves none of them: 13 of 24 loots in three sessions read "nothing" beside a stack of Stringy Wolf Meat. The strip's `inventory.revision` moves on every `BAG_UPDATE`; eating moves it too, hence the supply guard | **Current** - verified live: "took - an item onto a stack" (run `20260924T053651-ac99b2`) |
| **V98** | 24 Sep 2026 | **The fight's evidence clocks stand still while the character casts** - the last hit, swing and aim, against which re-aiming (3.5 s) and "in reach" (4.5 s) are measured | A cast stops the swings, so no evidence can arrive during one. Each Holy Light, about four seconds under pushback, aged the last hit past both windows: the fight then stepped at a wolf already in melee, levelled the camera and turned, eleven seconds without a swing after one heal, and ran out its 45 s with the wolf at 30% (run `20260924T052148-85c63f`) | **Current** - offline |
| **V99** | 24 Sep 2026 | **A leg whose end has come no closer by 2 yards in 12 s is stuck, moving or not** - handled as a stand-still is: unstick, then the planner again, the wall heuristic, and a blocked spot routed round | The server's creature mesh routes over slopes too steep for a character: below a hillside on the way to Deputy Willem it walked up and slid back for 147 s, always moving, so the stand-still test never fired; each slide past 2.5 s preempted the walk and the next one planned the same slope, until quest 18's accept step failed on its clock into the level 5-7 wolves (run `20260924T052148-85c63f`) | **Current** - offline |
| **V100** | 24 Sep 2026 | **Experience gained is a level objective's progress, whatever is selected** - each gain in a teaching episode on a grind closes a successful segment, as a quest counter's rise already did | The kill was counted only as `target_dead`, which also wants the corpse still selected, and the client clears the selection at the kill or the fight's loot moves it on. Kills inside grind teaching went uncounted: one episode ran 872 s through several and ended "stalled", 16 of 50 grind episodes succeeded, and acquire, the capability nearest a model, gained 2 qualified examples in six sessions. Experience is read off the strip (`char.xp_pct`, `char.level`) and moves only for kills, quests and exploration, all of them progress toward a level | **Current** - offline |
| **V101** | 24 Sep 2026 | **Four steps at an attacker biting in melee with no swing or hit between them: move off for it to follow out - back off 1.5 s, and every other time turn round and run 2 s clear** | A Mangy Wolf stood inside a tree trunk and bit a level 7 paladin from 69% to 35% while the fight stepped into the bark twelve times, gave up "unreachable", and began again three times over (run `20260924T053651-ac99b2`). Each step slid along the trunk, so the stand-still sidestep never fired. What chases the character comes out of the tree. Backing off alone keeps facing the attacker, and from a tree's roots with the wolf below it backed further up the trunk five times before the character died (run `...054447-632295`); forward is how a walk gets down off something | **Current** - offline |
| **V102** | 24 Sep 2026 | **A corpse sought with nothing selected is clicked where the hover proves it (dead, the killed unit's name) even when the client selects someone else during that hover** | After a kill the client cleared the selection and, the moment the hover landed on the dead Defias Thug, selected its living packmate: the probe reported "selection name changed during hover" and the loot gave up, three corpses in a row, and quest 18's bandanas with them (run `20260924T054447-632295`). With nothing selected there is no selection to protect; a right-click on a proven corpse loots it | **Current** - offline |
| **V103** | 24 Sep 2026 | **A delegated fight's experience is its kill to the tutor's judge**, whatever is selected afterwards; experience alone after any other action still is not | Three thugs killed by the tutor's own `COMBAT_PROFILE` were each judged "did not observe target_dead", the selection having moved to the next packmate, and the episode stalled on "repeated COMBAT_PROFILE 3 times without effect" (run `20260924T054447-632295`). A fight skill does nothing else that earns experience | **Current** - offline |
| **V104** | 24 Sep 2026 | **A meal on a step with known spawn points is eaten at least 25 yards from all of them** - the nearest such point on rings round the character, the one with most room on its ring; where none is known or found, where it stands | Eating in the middle of the level 5-7 wolf camp, a level 6-7 paladin was bitten at 26% health into a 45 s stalemate of heals, and at 30% when two more came (runs `20260924T053651-ac99b2`, `...050644-f9f9fa`). Past an aggro reach a meal is a meal. The tutor's rest episodes delegate to the same routine | **Current** - offline |
| **V105** | 24 Sep 2026 | **Combat takes the floor from a tutor holding any objective without a fight of its own running**, as it does from a walk (V35) | A fighting skill the tutor plays is no defence: the hunt (`GRIND_UNTIL`) was exempt from the combat interrupt because the scripted hunt fights for itself, and a level 7 paladin lost 51% to 0 in 30 s to a Defias Thug it had not selected while the tutor decided (run `20260924T055951-0c4439`). A delegated `COMBAT_PROFILE` is a fight of its own and is left to finish | **Current** - offline |
| **V106** | 24 Sep 2026 | **An object click nothing answers within 1.5 s - no cast, no loot window, no counter - is out of reach: turn toward it, step in, find it again and click, up to four times** | The first live gather: a crate of Milly's Harvest, hover-proved by its tooltip a few yards ahead, answered a right-click with nothing for 8 s, and the gather moved on (run `20260924T062715-7433c3`). The server drops a use from beyond its interaction distance without a word, as it did the Spirit Healer's right-clicks (V94) | **Current** - offline |
| **V107** | 24 Sep 2026 | **In combat, a selected unit that is neither attacking the character nor in melee is not the fight; and defending, what is attacking is chosen before anything of the wanted name** - plates in two passes, attackers first; `Tab` takes attackers only | The client selects units on its own. A Defias Thug at full health, neither biting nor near, stayed selected for 40 s while another thug killed the character: every fight counted the bystander as "already engaged", failed to prove its plate, and re-acquired a unit of the wanted name, the bystander again (run `20260924T064025-090aa8`). Unknown attack fields still count as engaged; a quest mob is still a fight when nothing else is attacking | **Current** - offline |
| **V108** | 24 Sep 2026 | **Arriving again where a teaching episode already arrived is not a useful action** - it counts toward the no-effect and repeat limits like any action that went nowhere | A tutor on Milly's Harvest delegated the walk to the objective's point and "arrived" for ten minutes at 6/8 crates: each arrival reset the no-effect count and a useful action is never a repeat, so the episode never handed over to the scripted gather (run `20260924T070341-9b2441`) | **Current** - verified live: "the tutor repeated TRAVEL_TO 3 times without effect", and the scripted gather finished Milly's Harvest (runs `20260924T071706-e10875`, `...073209-ca9cd2`) |
| **V109** | 24 Sep 2026 | **Two walks running that end with the character wedged - every unstick heading tried, none moved it - go home by hearthstone** (a stone on cooldown does nothing) | Inside Northshire Abbey, below Brother Neals' stairs and against a barrel, walk after walk ended "could not free the character" while the step went on failing (run `20260924T074713-f215ef`). A blocked walk the planner can still route round is not wedged. Found alongside it and not fixed: the strip paints map x/y only, and the Abbey's spiral stair has waypoints on different floors at the same x/y, so a 2D follower can take an upper floor's waypoint as reached from the floor below | **Current** - verified live (run `20260924T075209-e0395d`): "wedged 2 walks running: hearthstone home", out of the Abbey |
| **V110** | 24 Sep 2026 | **Which steps have already been retried after a rib is kept in the playhead**, so "a step that fails again after its rib is passed over" holds across sessions | The rule existed in memory only, and every fifteen-minute session gave a step its first failure afresh: quest 3905's hand-in, which Brother Neals' stair defeats, would have cycled between the stair and the level 5-7 wolves for good, with quests 6, 16, 2158, 60 and on waiting behind it. A step passed over keeps its quest in the log | **Current** - verified live (run `20260924T082110-0f56c6`): the hand-in, retried in session 43, was passed over; the rib now leads to quest 6 |
| **V111** | 24 Sep 2026 | **An operator stop waits for a fight in progress to end, up to 90 s**; a stop before the run is attached stays immediate | A session stopped mid-fight at 25% health left the character standing idle through the restart, and it died (run `20260924T075209-e0395d`) | **Current** - offline |
| **V112** | 24 Sep 2026 | **A step whose own skill runs out of attempts takes the step's own fail edge** (its rib, retried once, then passed over - V110) instead of stopping the session; a service skill failing on the way still stops it | The teaching launch allows one attempt per step and skill a session. Brother Neals' hand-in failed on the Abbey's stair ("no observed nameplate"), the session stopped, and the next one tried the step afresh with its 240 s clock back at zero: the step's timeout edge could never fire, and the route would have looped on it for good (run `20260924T081531-4249a4`) | **Current** - verified live: "TURNIN_QUEST out of attempts on ...3905_grape_manifest_turnin; failed over to ...grind_elwynn_5_7" |
| **V113** | 24 Sep 2026 | **Blind melee answers "facing the wrong way" with a quarter turn, always the same way**, not a half turn | A Mangy Wolf at the character's side, its plate unprovable, stayed at the side through eight half turns: forty seconds at 5% health without a hit, and the level 8 character died (run `20260924T082110-0f56c6`). Four quarters in one direction face anything; two halves can face neither side | **Current** - offline |
| **V114** | 24 Sep 2026 | **A corpse the loot routine has finished with is released from the selection** (Escape, only on a dead unit observed selected with no window open; a game menu that opens anyway is shut) | Left selected, the emptied corpse put the tutor in its loot situation after every kill: 43 tutor loot attempts in sessions 30-50, none taking anything, each a model call. A corpse the routine did not loot stays unselected for the tutor to find and take | **Current** - offline |
| **V115** | 24 Sep 2026 | **Before a meal, whenever the bags have changed, the character puts on anything in them better than what it has been given to wear** - scored from a catalog of this server's items (armour plus weighted stats; weapon damage per second), limited to what its starting proficiencies, level and masks allow, one-handers only, remembered per slot beside the playhead; a bind-on-equip question is answered yes | A level 8 paladin fought the night with its Worn Mace (1-3 damage) and starting clothes while its bags held a Militia Hammer (3-6), a Pikeman Shield (55 armour), Loose Chain Gloves (48), a belt, a cloak and shoes: quest rewards and loot never put on (run `20260924T090629-93a85b`). The strip does not say what is worn, so a slot never filled counts as empty; whatever comes off lands in the bags and is compared against the remembered score next time | **Current** - verified live (run `20260924T091648-c533a2`): "put on 8 of 8 upgrades" - Militia Hammer, Pikeman Shield, mail gloves, belt, cloak, shoes, robe, pants |
| **V116** | 24 Sep 2026 | **A rib ends as soon as the step it would rejoin is already done** - an objective whose quest is complete in the log, an accept whose quest is in it; not a hand-in, which cannot tell done from never taken without having watched it | Garrick Padfoot's hunt found nobody at his spawn and failed over into the level 5-7 rib; forty seconds later, still in the vineyard, the character killed him anyway and quest 6 read complete - and the rib ground on, the operator watching it kill Defias with two finished quests in the log (run `20260924T091648-c533a2`) | **Current** - offline |
| **V117** | 24 Sep 2026 | **A rib entered for any failure but deaths lasts five minutes** (`SHORT_RIB_S`, wall time, kept in the playhead), then rejoins its way back; a step that killed the character still earns the whole rib (a level, two deaths or its timeout). The ladder stays rib, retry once, pass over | A level cures a step that kills the character. It cures nothing for a quest giver out of reach, a named mob not up yet or a hand-in behind a stair, and a whole level of wolves at level 8 is an hour: the operator found Testvvi level 8 and still in Northshire after a night of such ribs, one of them grinding on with its quest already done (V116). Five minutes gives a respawn its time | **Current** - offline |
| **V118** | 24 Sep 2026 | **A teaching episode that combat cuts short after it has engaged something waits for the fight's verdict**: the next teaching episode's first look settles it - verified progress (a kill's experience on a level objective, a quest count) with the character alive within 180 s is a success; anything else, or a closing run, is the interruption it was | Combat takes the floor from the tutor (V105), and the kill its selection set up then landed outside its episode: grind teaching succeeded 3 times in 42 since, against 17 in 63 before, and every selection the tutor made in them went unqualified. The operator's ask: learn constantly, integrate whenever. Safety keeps V105; the data keeps its credit | **Current** - offline |
| **V119** | 24 Sep 2026 | **The character trains at its class trainer and puts what it learns on its bar by drags; the rotation uses it by role.** A trainer is chosen from a catalog of this server's trainers (`tools/gen_trainer_catalog.py`: class, side by faction masks, spawn, gossip line, offers) when it teaches something affordable the spellbook census lacks - the most of it, then the nearest; one visit a level. Train is pressed while painted (only while enabled) and each press is money spent. A new rank goes where the old one is on the bar and a new spell on any free slot (the operator's rule); which new spells earn a slot is by role read from each spell's effects - one aura, save, stun and last resort, a long buff per aura kind, every strike, no second heal or seal. The fight's profile is the bar census's spells by role: save or stun before a heal, a last resort under 15%, auras and blessings kept across fights until a death, a strike that spends a seal re-seals. The strip's schema 15 gives its 4-bit header a revision byte (code 0: the number follows) and paints a one-entry-per-paint bar census (spell id, button) and spellbook census (spell id, passive, button, or the tab or page button toward it), the spellbook's visibility and a held cursor. Training runs as a routine, never the tutor's | A level 8 paladin fought the night on Seal of Righteousness and Holy Light rank 1, losing to two wolves at once, while its trainer taught Devotion Aura, Blessing of Might, Judgement, Divine Protection and Holy Light rank 2 for five silver. The server replaces ranks on the bar only for spells whose skill line names a successor (auras, seals): Holy Light rank 2 stays off the bar until dragged. The operator's call: "replacement spells should replace respective skills on the bar, and new skill just order them however it doesnt matter" | **Current** - verified live (sessions 55-61): 7 spells bought at Brother Wilhelm, four dragged onto the bar, the aura and blessing pressed in fights; paladin auras are forms on 2.4.3 (active icon on the bar), spellbook buttons are numbered down the columns |
| **V120** | 24 Sep 2026 | **Routes are planned on ground a character can climb**: the sidecar asks the navmesh with the filter the server gives the bots it drives itself - ground and water, water twenty times as costly, never lava, slime or the steep band (50-60 degrees) the mesh generator marks "walkable for mobs, unwalkable for players"; only where that finds no complete route is everything allowed, and the answer says so | Back in Northshire at level 9 after a death, the leg out of the vineyards to the Gray Forest Wolves crossed seven steep polygons up a 55 degree face; the character slid back down it for four minutes, every slide preempting the walk, until the watchdog failed the step (session 74, run `20260924T133800-b58ab2`). V99 only noticed such a slope; the mesh already knew it. Asked that way alone, the same leg climbed a 44 degree slope instead, and session 76 slid on it for three minutes: over 13,400 ticks of sessions 60-76 the share of moving ticks spent falling rose with the slope underfoot, 2-7% below 25 degrees, 20-26% from 30 to 45, 37-53% above. With the slope costs the leg goes round by the road, 1,077 yards for 424, no stretch climbing more than one in two; the guide's 110 legs all still plan complete and grow 1.7% in all (45 had crossed steep ground, 38 had swum) | **Current** - offline |
| **V121** | 24 Sep 2026 | **A grey target gone from the selection below half its health is a kill** - one at or below the server's grey level for the character (`MaNGOS::XP::GetGrayLevel`), whose kill grants no experience to prove it; the loot that follows proves the corpse by hover as ever | Back in Northshire at level 9, six level 3-4 Defias were each last seen at 3-34% and settled "lost": the client clears the selection at a kill, and the fight's other proof, experience, cannot come for a grey unit. None was looted, while the next training wants silver (session 74, run `20260924T133800-b58ab2`). A vanish at full health is still lost | **Current** - offline |
| **V122** | 24 Sep 2026 | **A press counts only when the client answers it** - a cast, the global cooldown or the slot's own cooldown within 0.8 s; unanswered, everything the press stamped (its clock, a lasting buff's, a save's heal window, a heal's watch) is undone and the rotation chooses again. A first look after 1.4 s cannot tell and keeps the press, and a third unanswered press of one slot in a row is kept as well: an aura already up or a Judgement out of reach would otherwise hold the rotation on that row for the whole fight (session 78 pressed Devotion Aura four times running, Judgement four) | A level 9 paladin lost to one level 9 Defias Bandit, whose Snap Kick stuns for two seconds every five to nine: a seal pressed under the stun was stamped as up, Judgement stayed unusable for 25 s while the character swung unsealed, and at 12% a Holy Light pressed under the next stun held the heal row off for its 2.5 s watch while the rotation sealed and judged until it died (run `20260924T140621-fc3531`) | **Current** - offline |
| **V123** | 24 Sep 2026 | **A corpse is searched for past a living selection, and nearer the character** - with a living unit selected after a kill, the corpse is found by a dead hover of the killed unit's name, as with nothing selected (a right-click loots a corpse whatever is selected); the probes go 8 round the last plate, 8 on the centre line, then 6 beside the character's own model, 24 in all; in combat 6, and none below half health | Of the kills on 24 September (runs from 10:00), 39 corpses went unlooted against 65 looted: 20 because the client had moved the selection on at the kill to a living packmate of the same name, which the search refused as not dead, and 20 because 16 probes round the plate never reached the centre line or the ground beside the character - in Fargodeep Mine a kobold lay against the character's hip, the camera pulled in by the walls (run `20260924T140621-fc3531`). Every corpse found had been within seven probes | **Current** - offline |
| **V124** | 24 Sep 2026 | **A guide leads to the next: the playhead records that a character finished a guide, and its next session starts on the one after it** (`NEXT_GUIDE`), the quests done carried across. The human 1-12 guide leads to a generated 12-20 guide for Westfall and Redridge (`jev.guide.cli human --min 12 --max 20`), in Westfall's map frame. An elite blocks a quest only when it is fought: a delivery's creature is the quest's taker | The 1-12 guide ends with Report to Gryan Stoutmantle, and a finished guide only waited ("guide finished") until the session ran out: a character would have stopped at Sentinel Hill at level 12. Generating the next band found two of Gryan's hand-ins left out as "an elite; not a solo fight". The 1-12 file is not regenerated: its bytes are the learner's corpus fingerprint | **Current** - offline (the 12-20 route: 111 supported steps of 162; flights and the Stormwind errands are not supported yet) |
| **V125** | 24 Sep 2026 | **A bag service sells what the character has no use for, not just the grey** - white and green gear, trade goods and recipes that no quest asks for or hands over (`surplus_prices` in the vendor catalog), less gear worth wearing now or at a later level (`jev.world.gear.keep`); every such stack, since the character is at the merchant anyway. Blue and better never | Session 80 reached Joshua Maclure with 34 slots full - four Kobold Mining Shovels, the replaced Militia Hammer, three spare cloaks, five stacks of Stringy Wolf Meat, a schematic - and one grey sword: "no confirmed sale-eligible junk can make enough bag space", three times, while every kill after went unlooted ("bags are full"). Those stacks sell for about seven silver, and training at level 12 is ten a spell. The merchant is now visited at two free slots, not none: session 81 left four kills unlooted on its way to one. A visit that found nothing to sell waits until a slot frees and fills again, or more than two are free | **Current** - offline |
| **V126** | 24 Sep 2026 | **The character binds its hearthstone at the inn nearest the guide's work** (`BIND_HEARTH`, a routine like training): when an innkeeper of its side stands within 500 yards of the current step and home is unknown or more than 900 yards from it - talk to the innkeeper, choose "Make this inn your home.", accept the confirmation. Home is remembered beside the playhead, and so is wherever a hearthstone sets the character down; the vendor catalog lists this server's 70 innkeepers with their sides | A human's stone starts bound to Northshire Abbey, and each of the day's four recoveries by hearthstone (a death trap, a wedged walk) sent a level 9 questing round Goldshire and Fargodeep back there, the last onto the vineyard hill session 74 could not leave. From Westfall that walk back is 3,000 yards | **Current** - offline |
| **V127** | 24 Sep 2026 | **Flights.** The strip's schema 16 paints the flight master's map one node per paint: its name hashed as names are, whether it is here or somewhere to fly, and its button. A node is known by the hash painted as here where its flight master stands, remembered beside the playhead; only visited nodes can be flown to, the game's own rule. An unvisited flight master within 150 yards is visited (`DISCOVER_FLIGHT`, a routine). A walk of 1,500 yards or more flies its long part when a flight master stands within 600 yards of its start and a visited node near its end saves a quarter of the time or more; anything short of landing walks on from wherever the character is. The vendor catalog lists flight masters with the gossip line that opens each one's map; the addon still only paints (`TakeTaxiNode` is forbidden) | The Westfall-Redridge guide crosses Elwynn five times, up to 4,266 yards a crossing, about ten minutes a walk; Sentinel Hill to Lakeshire is about four minutes by gryphon and a walk at each end | **Current** - the schema 16 addon installed at 16:10 and painting on the live client (the map closed: `ui.taxi` false, no census); flights themselves not yet tried at a flight master |
| **V128** | 24 Sep 2026 | **In a fight, mana is kept back for a heal and the save behind it.** A seal, strike or buff that would leave less than the class's heal plus its save costs waits while in combat; out of combat nothing is held back, and a class without a heal is unaffected | Re-sealing after every Judgement (40 mana a seal) left a level 10 paladin at 54 of 300 mana, short of Holy Light's 60, and it died under its own Divine Protection with nothing to heal with (session 88) | **Current** |
| **V129** | 24 Sep 2026 | **A hybrid dispatch for the playing body, on trial against the tutor.** `--play-dispatch hybrid`: the guide's own routine takes an ordinary objective first; the tutor takes the one the routine just failed and a fixed one in four of the rest, chosen by a hash of the arm so the sample cannot follow the outcome. Sessions alternate two and two between the arms, and the winner is adopted by the plan's rule (hybrid if it earns 1.4 times the experience an hour or more, dies no more often, and keeps 40% of the teaching examples an hour) | Teach mode spends about 21% of play time waiting on the tutor, and the tutor-free runs levelled 1.6-5.5 times faster at levels 2-3; the student needs the tutor's examples, and the failures are where they teach most (docs/plans/nine-hour-session.md) | **Trial** - A/B from session 89 |
| **V130** | 24 Sep 2026 | **A person at the desk pauses the bot.** Every `SendInput` is stamped, in the process and in a file the next session reads; input Windows saw more than 300 ms after the bot's own is not the bot's, and a second such input within ten seconds of the first is a person. While a person has used the desktop in the last ten minutes nothing is pressed, no window is raised, the running skill is cancelled, the watchdog's clock is held, and a session starting then waits instead of failing | In the minute before the operator's stop on 24 September the bot pulled the game back to the foreground eight times while the operator was using the machine. Measured live: the bot's own input moves the last-input time (67 updates in 45 s of play) and a held key does not repeat it (gaps of up to 17 s), so a stamp per injection separates the two. A lone stray input 391 ms after the bot's own, with nobody at the desk and nothing for minutes after, paused session 91 mid-fight and the character died: one input alone is logged, not obeyed | **Current** - verified live in session 96 (00:54): two bursts of small mouse moves from another process paused it inside two seconds ("input 1625 ms after the bot's own; alone", then "375 ms ...; a person"), the walk under way was cancelled, and it resumed 30 s after the last input (a test window). Five sparse single moves were not seen: the bot's own input lands between them and each sample; a hand makes events every few milliseconds |
| **V131** | 24 Sep 2026 | **A controls generation keeps what still means the same.** The learner keeps each generation's manifest (from the session playing under it, and from every run's `play-config.json` as it is ingested). A capability's corpus is its records in the current generation plus earlier ones whose action rests on the same part of both manifests: a routine still available, a key with the same binding, a slot with the same contents, a click or camera move under the same pointer rules. A generation whose manifest is unknown stays apart, as before | The whole manifest changed five times in two days - four new routines and one new slot field - and each change started every capability's corpus again: 234 qualified examples sat in one generation and 15 in the current, so no student could reach its gates. Pooled, acquire has 85 qualified examples from 23 runs instead of 2, and rest 133 from 45 instead of 7 | **Current** - offline; live from session 91 |
| **V132** | 24 Sep 2026 | **Only a step's own work is an attempt at it.** A skill that ends aborted or timed out counts against its step only when the step armed it (a `guide.` rule); a fight, a meal or a service on the way does not, as the policy's own comment on reflexes already said | A fight whose target vanished counted as a first attempt at a quest accept, so the accept's first timeout made two and "quest not offered" failed the step over: a level 11 walked 1,558 yards to a grind and back for a quest that was offered, about five minutes (session 90) | **Current** - from session 93 |
| **V133** | 24 Sep 2026 | **A quest accept or hand-in is timed for its work, not its walk.** Skills done in front of an NPC (`walk_free`: `ACCEPT_QUEST`, `TURNIN_QUEST`) have sixty seconds for the looking, clicking and dialog; the time the body spends walking inside them is not charged, as a walk is bounded by travel's own timeout and stuck recovery | A step counts as reached a hundred yards out (0.03 of Elwynn's map). After selling in the Lion's Pride Inn cellar, the accept for Marshal Dughan outside spent its sixty seconds on the cellar stairs and timed out; the retry's walk took 155 turns with 18 stuck (session 90) | **Current** - from the next session |
| **V134** | 25 Sep 2026 | **Merchants are ranked by their walk, not their distance.** The twelve nearest in a straight line are planned on the navmesh (no narrower window: from the vineyards the two nearest were up a tower, 982 and 1,209 yards of climbing, a window of 100 yards round the nearest left Goldshire's out and both climbs failed, session 101), and each costs its path's length plus 15 yards a corner, or three times its distance when no route reaches it; merchants that answered before still come first | Goldshire's warlock trainer sells too, from the inn's cellar: the nearest in a straight line from the south, and walking back out took 155 turns and 18 stuck events, then cost a quest accept its budget (session 90). Planned from where that service began, the cellar costs 818 and the smith at the forge 571; twelve plans take a few milliseconds | **Current** - offline; live from the next session |
| **V135** | 25 Sep 2026 | **A passed-over hand-in is made on the way.** When a later step brings the character within the arrival radius of a hand-in that failed and was passed over, and its quest is complete in the log, the playhead detours to it with the current step as its way back. Handed in, it returns; failed, it returns at once without a rib. Each hand-in gets one detour ever, kept among the retried steps as `detour:` + its id. Not from a grind, not in a fight, not from a detour | Two complete quests sat in the log for good after their hand-ins were passed over: Kobold Candles, whose William Pestle stands twenty yards from Marshal Dughan, whom the guide visits again and again, and Grape Manifest in Northshire (25 September) | **Current** - from the next session |
| **V136** | 25 Sep 2026 | **The character's height is tracked along the navmesh.** The radio paints only map x and y, and 2.4.3 gives an addon no height. Twice a second while positions are read, the surface under the new position nearest the last height becomes the height, and plans and re-plans start from it; a surface found beside the position instead of under it is an edge left behind, and the floor is looked for 3-12 yards lower. Stairs over a walkable floor stay ambiguous (the lower floor is nearer) | In the Lion's Pride Inn one x and y is three places (the hall at 57, the floor above at 64, the roof at 74.5). A plan started in the hall walked the character round the floor above William Pestle for four minutes, then five more failing to walk out to Marshal Dughan, and both hand-ins were passed over (session 95). Replayed, the inn's landing sits over walkable hall, so that particular climb is not seen - the tracker helps where stairs are solid underneath and after falls | **Current** - offline; live from session 96 |
| **V137** | 25 Sep 2026 | **A learned passage belongs to its floor.** A passage (where walking was stopped, and the point the escape went by) now keeps the height of the route there, and a route only takes it at that height (within 4 yards, as blocked spots already did); the same x and y on another floor is another passage. Passages learned before heights were kept get the navmesh's floor under their spot, once, when there is one floor; over several they are not taken | Beside William Pestle in the Lion's Pride Inn a passage learned escaping one floor sent the escape twelve yards west towards the stairs, and every later route to him was bent through it: the character walked the floor above him for four minutes (session 95). Of 113 passages, 90 lie over one floor and keep working, 14 over several are set aside (the inn's among them), 9 have no floor under them | **Current** - from the next session |
| **V138** | 25 Sep 2026 | **The watchdog holds its clock while a service runs.** A meal, a merchant, a trainer, a bind or a flight master (the skills the step's own clock already stands still for, `Tracker.serving`) is not a stall: each is bounded by its own timeout | Session 101's walk of 389 yards to a merchant, after the tutor's own attempt at the bag objective, was failed over as "no quest or experience progress" and the step (Pie for Billy) went to a grind | **Current** - from the next session |

### Standing constraint
**Windows has one system cursor per session.** No input method — `SendInput`, a dongle,
anything — lets the bot play while you use the same desktop. See V7.

### Carried over from the plan, unchanged
No DLL injection, no memory read/write, no packet forge, no speed or teleport. Control is
virtual HID; sense is screen capture plus an addon that paints and never actuates. Offline
server only. (PLAN §2.)
